# Elves Command:

### 1. Create new Model
```bash
source .venv/bin/activate
elves create-model model-name
```

## Updating Commands

Install in editable mode (so CLI works)

```bash
pip install -e .
```